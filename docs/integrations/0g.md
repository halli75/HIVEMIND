# 0G Integration

## Targets

- 0G Compute for active inference.
- 0G Storage for agent state, memory, and run logs.
- 0G Chain for the minimal iNFT mint on Galileo testnet.

## Current Status

- Contract network config points at 0G Galileo testnet by default through `ZERO_G_RPC_URL`.
- Live 0G Compute is wired behind `ZeroGComputeInferenceProvider` and the hybrid top-N inference path.
- `contracts/scripts/mint-inft.ts` fetches the current winner, encrypts the strategy with AES-256-GCM, uploads to 0G Storage, then mints `HivemindINFT` on Galileo.
- `/mint` delegates to that script and now returns `storage_unavailable` when 0G Storage fails before minting.
- `/state.proof.inft` is updated after a successful mint; until then it shows the configured Galileo contract with `token_id: null`.

## Live Proof Rehearsal - 2026-04-29

Evidence directory: `runs/proof-20260429-161627/` (ignored, non-secret).

| Step | Result | Evidence |
| --- | --- | --- |
| `/health` | Passed | `run_mode=local_axl+live_0g` |
| `/scenario` | Passed with partial fallback | 10 top-N 0G Compute attempts, 9 live completions, 1 fallback from HTTP 429, avg latency 7607.476 ms |
| Winner | Passed | `agent-014` selected from scenario `e2e-proof-20260429` |
| `/mint` encryption | Passed | Winner fetched, AES-256-GCM encryption completed, local demo keyfile written under ignored `contracts/runs/inft-keys/` |
| 0G Storage upload | Blocked externally | 4 upload attempts returned HTTP 503 from the 0G Storage indexer |
| 0G Chain mint | Not started | The script refuses to mint until encrypted strategy storage succeeds |

Current blocker: 0G Storage testnet/indexer availability. This is intentionally surfaced as HTTP 503 with `detail.status="storage_unavailable"` so the dashboard/demo can distinguish external storage downtime from a contract or encryption bug.

## Turbo Indexer Retry - 2026-04-29

The alternate turbo indexer moved past HTTP 503, selected storage nodes, and prepared the upload. The remaining failure changed to a Flow contract submit revert before iNFT minting:

- Endpoint: `https://indexer-storage-testnet-turbo.0g.ai`
- Storage nodes selected: `34.83.53.209:5678`, `34.169.28.106:5678`
- Flow address: `0x22e03a6a89b950f1c82ec5e74f8eca321a105296`
- Error: `Failed to submit transaction: ProviderError: execution reverted`
- Static call probe: exact and 10 percent padded storage fees both reverted with no data.
- Evidence directory: `runs/proof-turbo-20260429-165426/` (ignored, non-secret).

Sanitized escalation notes are in `docs/integrations/0g-escalation.md`.

## Official Reference Points

- Galileo testnet chain ID: `16602`.
- Development RPC placeholder: `https://evmrpc-testnet.0g.ai`.
- 0G Storage supports Go and TypeScript SDKs for upload/download workflows.
- 0G Compute exposes inference through web UI, CLI, SDK, and direct API access.

References:

- https://docs.0g.ai/developer-hub/testnet/testnet-overview
- https://docs.0g.ai/developer-hub/building-on-0g/storage/sdk
- https://docs.0g.ai/developer-hub/building-on-0g/compute-network/inference

## Remaining Integration Steps

1. Retry `POST /mint` when 0G Storage recovers.
2. Confirm the upload returns a real `rootHash` and storage transaction.
3. Mint `HivemindINFT` on Galileo with the verified storage URI and hash.
4. Confirm `/state.proof.inft.status == "minted"` and `token_id` is populated.
5. Add explorer and storage readback evidence to the demo notes.

## Safety

- Use funded testnet wallets only.
- Never commit `DEPLOYER_PRIVATE_KEY`, 0G bearer tokens, or provider secrets.
- Treat 0G addresses as testnet-drift-prone; verify addresses before final demo.
